int curr = n;
    // vector<int> path;
    // while (curr != -1)
    // {
    //     path.push_back(curr);
    //     curr = parent[curr];
    // }
    // reverse(path.begin(), path.end());
    // for (auto p : path)
    // {
    //     cout << p << " ";
    // }